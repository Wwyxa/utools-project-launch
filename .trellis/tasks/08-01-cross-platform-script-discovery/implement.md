# Execution plan

1. Extend shared types and the browser bridge fallback for discovery candidates
   and the `makefile` source.
2. Add preload shell resolution, apply it to execution/tool detection, and add
   static Makefile discovery plus a discovery bridge method.
3. Change store inspection/import actions so script replacement is explicit,
   additive, and deduplicated.
4. Add a compact selectable candidate panel to the project form and localize
   its labels.
5. Add preload/discovery regression tests and run type, build, and focused
   validation commands.
