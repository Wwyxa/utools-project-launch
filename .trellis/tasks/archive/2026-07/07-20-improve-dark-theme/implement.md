# 暗色界面视觉层级实施计划

## Steps

- [x] 读取实现前规范，确认全局 CSS 的项目约束。
- [x] 在 `src/index.css` 中重标定 `.dark` 表面、文字、边框、工具栏、筛选控件和滚动条颜色。
- [x] 保持亮色令牌、代码高亮令牌及 Vue 组件结构不变。
- [x] 运行 `npm run lint` 和 `npm run build`。
- [x] 启动本地开发服务，在桌面与紧凑窗口检查仪表盘和项目详情的默认、hover、active 与弹层状态。
- [x] 触发项目卡片、备忘录隐藏按钮、脚本下拉菜单和终端上下文菜单，并静态核对 Git 文件行操作组的共享令牌。
- [x] 执行 Trellis 质量检查并修复本次改动引入的问题。

## Validation

- `npm run lint`
- `npm run build`
- 浏览器检查：仪表盘背景/工具栏/卡片层级、筛选标签状态、项目详情与设置页面、隐藏操作按钮、浮动菜单、紧凑窗口溢出。

## Risk And Rollback Points

- 主要风险是共享令牌在次要页面产生意外低对比；通过项目详情与设置页抽查发现。
- 首次改动只触及 `src/index.css`，若局部组件对比异常，再做最小的组件级修正。

## Validation Notes

- 已在桌面与 768px 视口检查仪表盘、项目详情、设置、亮色切换及水平溢出。
- 已实际触发 tiny/标准卡片隐藏操作、备忘录行内按钮、脚本下拉菜单和终端上下文菜单。
- 浏览器环境没有 uTools 文件桥，示例项目也没有 Git 状态数据，无法生成 Git 文件行；已静态确认该操作组复用本次调整过的语义表面、边框和文字令牌。
